---
import '../styles/global.css';
import { externalDemos } from '../data/externalDemos';

const categoryPillClass: Record<string, string> = {
  'Open-source ERP': 'bg-amber-100 text-amber-900',
};

const layerVariant: Record<'reaches' | 'fails' | 'opaque', { pillBg: string; cardBorder: string; pillText: string }> = {
  reaches: {
    pillBg: 'bg-emerald-100 text-emerald-800',
    cardBorder: 'border-emerald-200',
    pillText: '✓ Reaches',
  },
  fails: {
    pillBg: 'bg-rose-100 text-rose-800 ring-1 ring-rose-300',
    cardBorder: 'border-rose-300',
    pillText: '✗ Stops here',
  },
  opaque: {
    pillBg: 'bg-slate-200 text-slate-700',
    cardBorder: 'border-slate-300',
    pillText: '· No DOM',
  },
};

// Short labels for the tab strip — `title` is too long for a tab chip.
const tabLabel: Record<typeof externalDemos[number]['id'], string> = {
  'odoo-spreadsheet': 'Odoo Spreadsheet',
  'business-one-google': 'SAP B1 · Google sign-in',
};

// odoo-spreadsheet is the exemplar and loads by default. Keep this index in
// sync with the `tabLabel` ordering if more demos are added later.
const defaultTabId = 'odoo-spreadsheet';
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>External demos — Bot Arena</title>
    <meta
      name="description"
      content="Two ways Playwright runs out of road on public SaaS: canvas-rendered application surfaces (Odoo Spreadsheet) and third-party IdP gateways with reCAPTCHA (SAP Business One Cloud + Sign in with Google)."
    />
  </head>
  <body class="min-h-screen bg-slate-50 text-slate-900">
    <header class="border-b border-slate-200 bg-white">
      <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
        <a href="/" class="inline-flex items-center gap-2 text-lg font-semibold tracking-tight">
          <img src="/favicon.svg" alt="" class="h-6 w-6" />
          <span>Bot Arena</span>
        </a>
        <nav class="flex gap-4 text-sm text-slate-600">
          <a href="/comparison" class="hover:text-slate-900">Comparison</a>
          <a href="/report" class="hover:text-slate-900">Failure report</a>
          <a href="/other-usecases" class="hover:text-slate-900">Other use-cases</a>
          <a href="/external" class="text-slate-900 font-medium">External demos</a>
          <a href="/about" class="hover:text-slate-900">About</a>
          <a href="https://github.com/vaclavnovotny/bot-arena" class="hover:text-slate-900">GitHub</a>
        </nav>
      </div>
    </header>

    <main class="mx-auto max-w-3xl px-6 py-12">
      <p class="text-xs font-semibold uppercase tracking-widest text-slate-500">Out of arena</p>
      <h1 class="mt-2 text-3xl font-bold tracking-tight sm:text-4xl">
        Where Playwright runs out of road in public SaaS
      </h1>
      <p class="mt-3 max-w-3xl text-slate-600">
        Selector-based automation runs out of road in two distinct ways on the public web:
        the <strong>application surface</strong> goes opaque when working areas render to
        <code class="rounded bg-slate-100 px-1 py-0.5 text-[11px]">&lt;canvas&gt;</code>, and the
        <strong>gateway</strong> slams shut when third-party identity providers actively reject
        WebDriver-controlled browsers before the SaaS even loads. Pick a case study below.
      </p>

      <!-- Tab strip — one tab per externalDemos entry. -->
      <div
        role="tablist"
        aria-label="External demos"
        class="mt-8 flex flex-wrap gap-1 border-b border-slate-200"
      >
        {externalDemos.map((d) => {
          const isDefault = d.id === defaultTabId;
          return (
            <button
              type="button"
              role="tab"
              id={`tab-${d.id}`}
              aria-controls={`panel-${d.id}`}
              aria-selected={isDefault ? 'true' : 'false'}
              data-tab-target={d.id}
              class:list={[
                'group relative -mb-px rounded-t-md px-4 py-2 text-sm font-medium transition',
                'border border-b-0',
                isDefault
                  ? 'border-slate-200 bg-white text-slate-900'
                  : 'border-transparent text-slate-500 hover:text-slate-900 hover:bg-slate-100',
              ]}
            >
              {tabLabel[d.id]}
            </button>
          );
        })}
      </div>

      {externalDemos.map((d) => {
        const isDefault = d.id === defaultTabId;
        return (
          <section
            role="tabpanel"
            id={`panel-${d.id}`}
            aria-labelledby={`tab-${d.id}`}
            data-tab-panel={d.id}
            hidden={!isDefault}
            tabindex="0"
          >
            {d.id === 'odoo-spreadsheet' && (
              <div class="mt-6 grid gap-3 sm:grid-cols-3">
                <div class="rounded-lg border border-slate-200 bg-white p-4">
                  <p class="text-xs font-semibold uppercase tracking-wide text-slate-400">The pattern, at scale</p>
                  <p class="mt-1 text-2xl font-bold tracking-tight text-slate-900">900M+</p>
                  <p class="mt-0.5 text-[11px] font-semibold uppercase tracking-wide text-slate-500">monthly users on a canvas-rendered spreadsheet</p>
                  <p class="mt-2 text-xs leading-relaxed text-slate-600">
                    <a href="https://electroiq.com/stats/google-workspace-statistics/" target="_blank" rel="noopener" class="underline decoration-slate-300 underline-offset-2 hover:decoration-slate-700">Google Sheets alone</a>;
                    <strong>Microsoft Excel</strong> adds another 750M+
                    (<a href="https://earthweb.com/excel-users/" target="_blank" rel="noopener" class="underline decoration-slate-300 underline-offset-2 hover:decoration-slate-700">Microsoft, 2017 baseline</a>),
                    <strong>Smartsheet</strong> runs at <strong>85% of Fortune 500</strong>.
                    <a href="https://workspaceupdates.googleblog.com/2021/05/Google-Docs-Canvas-Based-Rendering-Update.html" target="_blank" rel="noopener" class="underline decoration-slate-300 underline-offset-2 hover:decoration-slate-700">Google Docs migrated to canvas in May 2021</a>
                    and broke a swathe of Chrome extensions in the process — selector tools only see the chrome around the grid.
                  </p>
                </div>
                <div class="rounded-lg border border-slate-200 bg-white p-4">
                  <p class="text-xs font-semibold uppercase tracking-wide text-slate-400">The category</p>
                  <p class="mt-1 text-2xl font-bold tracking-tight text-slate-900">71%</p>
                  <p class="mt-0.5 text-[11px] font-semibold uppercase tracking-wide text-slate-500">of FP&amp;A teams own EPM tools — and still run on spreadsheets</p>
                  <p class="mt-2 text-xs leading-relaxed text-slate-600">
                    <a href="https://www.financialprofessionals.org/training-resources/resources/articles/Details/why-aren-t-fp-a-teams-using-the-epm-tools-they-bought" target="_blank" rel="noopener" class="underline decoration-slate-300 underline-offset-2 hover:decoration-slate-700">2025 AFP FP&amp;A Benchmarking Survey</a>.
                    A separate 700-leader survey: <strong>40% of businesses</strong> still manage half their financial data
                    manually; <strong>26%</strong> manage the majority manually
                    (<a href="https://www.fintechstrategy.com/blog/2025/10/20/finance-teams-stuck-in-spreadsheets-as-digital-transformation-stalls/" target="_blank" rel="noopener" class="underline decoration-slate-300 underline-offset-2 hover:decoration-slate-700">FinTech Strategy, 2025</a>).
                    Spreadsheets stay the de facto ERP surface — and vendors ship canvas spreadsheets to plug the gap.
                  </p>
                </div>
                <div class="rounded-lg border border-emerald-200 bg-emerald-50/40 p-4">
                  <p class="text-xs font-semibold uppercase tracking-wide text-emerald-700">Our exemplar</p>
                  <p class="mt-1 text-2xl font-bold tracking-tight text-slate-900">13M+</p>
                  <p class="mt-0.5 text-[11px] font-semibold uppercase tracking-wide text-slate-500">Odoo users — the canvas-ERP pattern with a public trial</p>
                  <p class="mt-2 text-xs leading-relaxed text-slate-600">
                    <strong>40% YoY growth</strong>, 7,000 new clients/month, <strong>€5 billion valuation</strong>
                    (<a href="https://www.odoo.com/blog/odoo-news-5/odoo-sa-announces-a-500-million-transaction-increasing-the-belgian-unicorns-valuation-to-5-billion-1508" target="_blank" rel="noopener" class="underline decoration-slate-300 underline-offset-2 hover:decoration-slate-700">Odoo SA, Nov 2024</a>).
                    ~<strong>15% of the SMB ERP market</strong>, projected 25% by 2027. Their Spreadsheet ships in every
                    Enterprise edition — and the free trial lets us demonstrate the failure publicly, against a real grid,
                    without violating anyone's terms.
                  </p>
                </div>
              </div>
            )}

            <div id={d.id} class="mt-8 rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
              <p class="text-[11px] font-semibold uppercase tracking-widest text-emerald-700">
                {d.id === 'odoo-spreadsheet' ? 'Exemplar · live demo' : 'Live demo'}
              </p>
              <div class="mt-1 flex flex-wrap items-baseline gap-x-3 gap-y-1">
                <h2 class="text-xl font-semibold text-slate-900">{d.title}</h2>
                <span class={`rounded-full px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wide ${categoryPillClass[d.category] ?? 'bg-slate-100 text-slate-700'}`}>
                  {d.category}
                </span>
              </div>
              <p class="mt-1 text-xs text-slate-500">
                <a href={d.demoUrl} target="_blank" rel="noopener" class="underline decoration-slate-300 underline-offset-2 hover:decoration-slate-700">
                  {d.demoUrl}
                </a>
              </p>

              <h3 class="mt-6 text-xs font-semibold uppercase tracking-wide text-slate-500">Goal</h3>
              <p class="mt-1 text-sm text-slate-800">{d.goal}</p>

              <h3 class="mt-5 text-xs font-semibold uppercase tracking-wide text-slate-500">Steps</h3>
              <ol class="mt-1 list-decimal space-y-1 pl-5 text-sm text-slate-800">
                {d.steps.map((s) => <li>{s}</li>)}
              </ol>

              <h3 class="mt-5 text-xs font-semibold uppercase tracking-wide text-slate-500">Problem</h3>
              <p class="mt-1 text-sm leading-relaxed text-slate-800" set:html={d.problem} />

              <h3 class="mt-5 text-xs font-semibold uppercase tracking-wide text-slate-500">
                Where Playwright bounces off
              </h3>
              <ol class="mt-2 space-y-2">
                {d.layers.map((layer) => {
                  const v = layerVariant[layer.status];
                  return (
                    <li class={`rounded-md border ${v.cardBorder} bg-white p-3`}>
                      <div class="flex flex-wrap items-center gap-2">
                        <span class={`inline-flex flex-none items-center rounded-full px-2.5 py-0.5 text-[10px] font-semibold uppercase tracking-wide ${v.pillBg}`}>
                          {v.pillText}
                        </span>
                        <span class="text-sm font-medium text-slate-900">{layer.name}</span>
                      </div>
                      <p class="mt-1.5 text-xs leading-relaxed text-slate-600" set:html={layer.detail} />
                    </li>
                  );
                })}
              </ol>
              <p class="mt-2 text-xs italic text-slate-500">{d.aivaFootnote}</p>

              <div class="mt-6 flex flex-wrap items-center gap-2">
                <span class="inline-flex items-center rounded-full bg-rose-100 px-2.5 py-0.5 text-[10px] font-semibold uppercase tracking-wide text-rose-800 ring-1 ring-rose-300">
                  ✗ Fails
                </span>
                <h3 class="text-xs font-semibold uppercase tracking-wide text-slate-500">
                  Playwright recording <span class="ml-1 font-normal normal-case tracking-normal text-slate-400">({d.videoDuration})</span>
                </h3>
              </div>
              <video
                class="mt-2 w-full rounded-md border border-slate-200 bg-black"
                src={d.videoSrc}
                poster={d.posterSrc}
                controls
                preload="metadata"
                playsinline
              >
                <a href={d.videoSrc} class="text-sm text-slate-200 underline">Download the recording ({d.videoDuration})</a>
              </video>

              {d.stills && d.stills.length > 0 && (
                <div class="mt-4 grid gap-3 sm:grid-cols-3">
                  {d.stills.map((s) => (
                    <figure class="rounded-md border border-rose-200 bg-white p-2">
                      <img src={s.src} alt={s.caption} class="w-full rounded-sm border border-rose-100" />
                      <figcaption class="mt-2 text-[11px] leading-snug text-slate-600">{s.caption}</figcaption>
                    </figure>
                  ))}
                </div>
              )}

              {d.aivaVideoSrc ? (
                <>
                  <div class="mt-6 flex flex-wrap items-center gap-2">
                    <span class="inline-flex items-center rounded-full bg-emerald-100 px-2.5 py-0.5 text-[10px] font-semibold uppercase tracking-wide text-emerald-800 ring-1 ring-emerald-300">
                      ✓ Passes
                    </span>
                    <h3 class="text-xs font-semibold uppercase tracking-wide text-slate-500">
                      AIVA recording <span class="ml-1 font-normal normal-case tracking-normal text-slate-400">(same task — 2× speed)</span>
                    </h3>
                  </div>
                  <video
                    class="mt-2 w-full rounded-md border border-emerald-200 bg-black"
                    src={d.aivaVideoSrc}
                    controls
                    preload="metadata"
                    playsinline
                    data-default-rate="2"
                  >
                    <a href={d.aivaVideoSrc} class="text-sm text-slate-200 underline">Download the AIVA recording</a>
                  </video>
                  <p class="mt-2 text-xs text-slate-600">{d.aivaVideoCaption}</p>

                  <details class="mt-3 rounded-md border border-emerald-200 bg-emerald-50/40">
                    <summary class="cursor-pointer select-none px-3 py-2 text-xs font-medium text-emerald-900 hover:bg-emerald-100/60">
                      Show AIVA's step-by-step run log
                    </summary>
                    <div class="px-3 pb-3">
                      <img
                        src={d.aivaStepsImageSrc}
                        alt={`AIVA test-runs viewer screenshot showing each step the agent executed against ${d.title} (login credentials redacted).`}
                        class="w-full rounded-md border border-emerald-200"
                      />
                      <p class="mt-2 text-xs text-slate-600">{d.aivaStepsCaption}</p>
                    </div>
                  </details>
                </>
              ) : (
                <div class="mt-6 rounded-md border border-dashed border-emerald-300 bg-emerald-50/40 p-4">
                  <div class="flex flex-wrap items-center gap-2">
                    <span class="inline-flex items-center rounded-full bg-emerald-100 px-2.5 py-0.5 text-[10px] font-semibold uppercase tracking-wide text-emerald-800 ring-1 ring-emerald-300">
                      AIVA recording pending
                    </span>
                  </div>
                  <p class="mt-2 text-xs leading-relaxed text-slate-700">{d.aivaPendingNote}</p>
                </div>
              )}

              <details class="group mt-6 rounded-md border border-slate-200 bg-slate-50">
                <summary class="flex cursor-pointer select-none items-center justify-between px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-100">
                  <span>Show the Playwright test</span>
                  <span class="text-xs text-slate-500 group-open:hidden">expand</span>
                  <span class="hidden text-xs text-slate-500 group-open:inline">collapse</span>
                </summary>
                <pre class="overflow-x-auto rounded-b-md bg-slate-900 p-4 text-[12px] leading-relaxed text-slate-100"><code>{d.testCode}</code></pre>
                <p class="border-t border-slate-200 px-4 py-2 font-mono text-[11px] text-rose-700">{d.failureLine}</p>
              </details>
            </div>
          </section>
        );
      })}
    </main>

    <footer class="border-t border-slate-200 py-6 text-center text-xs text-slate-500">
      Demo target only. No analytics, no tracking.
    </footer>

    <script>
      // Tab switching: clicking a tab button shows its panel and hides the
      // others; the URL hash is updated so links like /external#business-one-google
      // are still bookmarkable. On load, the hash decides the active tab
      // (default falls through to whatever was server-rendered as visible).
      const tabs = Array.from(document.querySelectorAll<HTMLButtonElement>('[role="tab"][data-tab-target]'));
      const panels = Array.from(document.querySelectorAll<HTMLElement>('[data-tab-panel]'));

      function activate(id: string, { updateHash = true }: { updateHash?: boolean } = {}) {
        if (!tabs.some((t) => t.dataset.tabTarget === id)) return;
        for (const t of tabs) {
          const active = t.dataset.tabTarget === id;
          t.setAttribute('aria-selected', active ? 'true' : 'false');
          if (active) {
            t.classList.add('border-slate-200', 'bg-white', 'text-slate-900');
            t.classList.remove('border-transparent', 'text-slate-500', 'hover:text-slate-900', 'hover:bg-slate-100');
          } else {
            t.classList.remove('border-slate-200', 'bg-white', 'text-slate-900');
            t.classList.add('border-transparent', 'text-slate-500', 'hover:text-slate-900', 'hover:bg-slate-100');
          }
        }
        for (const p of panels) {
          p.hidden = p.dataset.tabPanel !== id;
        }
        if (updateHash) {
          // Replace state so the back button doesn't accumulate tab toggles.
          history.replaceState(null, '', `#${id}`);
        }
      }

      for (const tab of tabs) {
        tab.addEventListener('click', () => {
          const target = tab.dataset.tabTarget;
          if (target) activate(target);
        });
      }

      // Honour an incoming hash on load (deep-link to a specific demo).
      const initialHash = location.hash.replace(/^#/, '');
      if (initialHash) activate(initialHash, { updateHash: false });

      // Keep tabs in sync if the user navigates via browser history.
      window.addEventListener('hashchange', () => {
        const id = location.hash.replace(/^#/, '');
        if (id) activate(id, { updateHash: false });
      });

      // Honour each video's data-default-rate as its starting playback speed.
      // Re-apply on metadata + play because some browsers reset rate when the
      // resource finishes loading.
      for (const v of document.querySelectorAll<HTMLVideoElement>('video[data-default-rate]')) {
        const rate = Number(v.dataset.defaultRate);
        if (!Number.isFinite(rate) || rate <= 0) continue;
        const apply = () => { v.playbackRate = rate; };
        v.addEventListener('loadedmetadata', apply);
        v.addEventListener('play', apply);
        apply();
      }
    </script>
  </body>
</html>
